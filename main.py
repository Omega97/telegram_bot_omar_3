from src.omar_bot.bot import run_bot


if __name__ == "__main__":
    # This is the main entry point of the application.
    # It simply calls the function that builds and runs the bot.
    run_bot()
